services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    env_file: .env
    ports:
      - "8000:8000"

  worker:
    build:
      context: .
      dockerfile: Dockerfile.celery
    env_file: .env
